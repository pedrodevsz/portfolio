export interface Project {
    _id?: string
    title: string
    description: string
    image: string
    stack: string[]
    techHighlight: string
    githubUrl: string
    demoUrl: string
    createdAt?: string
}